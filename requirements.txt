# ═══════════════════════════════════════════════════════════
# SecureStorageInspector — Python Dependencies
# ═══════════════════════════════════════════════════════════
# Install with:  pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── Configuration (Phase 1) ──────────────────────────────
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

# ── Secure XML Parsing (Phase 2) ────────────────────────
defusedxml>=0.7.1,<1.0

# ── Web Framework (Phase 3) ─────────────────────────────
fastapi>=0.100,<1.0
uvicorn[standard]>=0.23,<1.0
python-multipart>=0.0.6,<1.0

# ── Database (Phase 3) ──────────────────────────────────
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0

# ── Task Queue (Phase 3) ────────────────────────────────
celery[redis]>=5.3,<6.0
redis>=5.0,<6.0

# ── API Security (Phase 3) ──────────────────────────────
slowapi>=0.1.9,<1.0

# ── Testing ──────────────────────────────────────────────
pytest>=7.0,<9.0
pytest-cov>=4.0,<6.0

# ── Phase 5+ dependencies (not needed yet) ───────────────
# weasyprint>=60,<62
# python-jose[cryptography]>=3.3,<4.0
